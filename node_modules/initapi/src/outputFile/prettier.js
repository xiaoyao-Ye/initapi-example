"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const prettier_1 = __importDefault(require("prettier"));
const defaultOptions = {
    // 解析器
    parser: 'typescript',
    // 解析规则
    // 单引号
    singleQuote: false,
    // 尾分号
    semi: true,
    jsxSingleQuote: false,
    trailingComma: 'none',
    bracketSpacing: true,
    bracketSameLine: false,
    rangeStart: 0,
    rangeEnd: Infinity,
    proseWrap: 'preserve',
    arrowParens: 'avoid',
    endOfLine: 'lf',
};
/**
 * TS/JS格式化代码
 * @param code 文件数据
 * @param options 格式化规则
 * @returns 格式化后的数据
 */
function format(code, options = defaultOptions) {
    return prettier_1.default.format(code, options);
}
exports.default = format;
